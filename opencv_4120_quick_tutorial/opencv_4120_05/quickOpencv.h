#pragma once

#include <opencv2/opencv.hpp>
using namespace cv;
using namespace std;

class QuickDemo {
public:
	void colotSpaceDemo(Mat& image);
	void mat_creation_demo(Mat& image);
	void pixel_visit_demo(Mat& image);
	void operate_demo(Mat& image);
};